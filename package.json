{
  "name": "rolebox",
  "version": "0.0.1",
  "description": "OpenCode custom agent role plugin — define roles via YAML config with custom prompts, models, skills, and permissions",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "fast-glob": "^3.3.0",
    "js-yaml": "^4.1.0"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": "^1.3.0"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.3.0",
    "@types/js-yaml": "^4.0.0",
    "bun-types": "latest",
    "typescript": "^5.7.0"
  }
}
